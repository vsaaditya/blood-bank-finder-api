# Database credentials come from environment variables (SPRING_DATASOURCE_URL, etc.)
# No hardcoded DB settings needed here!

# Less logging in prod — saves server resources
logging.level.org.springframework=ERROR

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never